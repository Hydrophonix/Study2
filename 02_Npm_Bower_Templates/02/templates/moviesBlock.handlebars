<div class="box">
    <div class="head">
      <h2>{{blockName}}</h2>
        <p class="text-right"><a href="#">See all</a></p>
    </div>

    {{#each movies}}
        {{> movie this }}
    {{/each}}

</div>
